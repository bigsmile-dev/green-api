{
  "name": "green-api-test-page",
  "version": "1.0.0",
  "description": "GREEN-API Test Page with Node.js Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "build:inline": "node build-inline.js",
    "build:vercel": "npm run build:inline",
    "build:prod": "npm run build && cd dist && npm install --production",
    "start:prod": "cd dist && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "green-api",
    "whatsapp",
    "api",
    "nodejs",
    "express"
  ],
  "author": "Lead Golang, NodeJS Developer",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 